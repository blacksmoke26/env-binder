# Advanced .env variables covering all EnvBinder.ts scenarios
# ==============================================================================
# COMPREHENSIVE ENVIRONMENT VARIABLE EXAMPLES
# ==============================================================================
# This section demonstrates all supported variable types and transformations
# by the EnvBinder class, including edge cases and complex scenarios.
# ==============================================================================

NODE_ENV=staging

# Boolean Values (covering all falsy scenarios)
ENABLE_FEATURE=true
DISABLE_FEATURE=false
NULL_BOOLEAN=null
UNDEFINED_BOOLEAN=undefined
EMPTY_BOOLEAN=""
ZERO_BOOLEAN=0
FALSY_BOOLEAN=false

# String Values with various edge cases
BASIC_STRING="hello world"
STRING_WITH_QUOTES='contains "quotes" inside'
EMPTY_STRING=""
NUMERIC_AS_STRING="123"
BOOLEAN_AS_STRING="true"
URL_AS_STRING="https://example.com"
ESCAPED_STRING="line1\nline2\ttabbed"
UNICODE_STRING="Café Münster"
SPECIAL_CHARS="!@#$%^&*()_+-=[]{}|;:',.<>?"

# Number Values (integers, decimals, scientific notation)
INTEGER_VALUE=42
NEGATIVE_INTEGER=-17
ZERO_NUMBER=0
DECIMAL_VALUE=3.14159
NEGATIVE_DECIMAL=-2.71828
SCIENTIFIC_NOTATION=1.23e5
PERCENTAGE_VALUE=0.95
LARGE_NUMBER=9223372036854775807
SMALL_DECIMAL=0.000001

# URL Values with various protocols and formats
HTTP_URL="http://example.com"
HTTPS_URL="https://secure.example.com"
FTP_URL="ftp://files.example.com"
WS_URL="ws://websocket.example.com"
WSS_URL="wss://secure.websocket.example.com"
URL_WITH_PORT="https://api.example.com:8443"
URL_WITH_PATH="https://cdn.example.com/assets/images"
URL_WITH_QUERY="https://search.example.com?q=test&limit=10"
URL_WITH_FRAGMENT="https://docs.example.com#section1"
LOCALHOST_URL="http://localhost:3000"
IP_URL="http://192.168.1.100:8080"

# Required Values (these must be present)
REQUIRED_API_KEY="sk-1234567890abcdef"
DATABASE_PASSWORD_REQUIRED="super-secret-password"
JWT_SECRET_REQUIRED="your-jwt-signing-secret-here"

# Time Duration Values (various units and formats)
TIME_SECONDS=30s
TIME_MINUTES=15m
TIME_HOURS=2h
TIME_DAYS=7d
TIME_WEEKS=2w
TIME_MONTHS=3M
TIME_YEARS=1y
COMPLEX_TIME="1h30m45s"
NEGATIVE_TIME=-5m
ZERO_TIME=0s
DECIMAL_TIME=1.5h

# Array Values (JSON format)
STRING_ARRAY='["apple","banana","cherry"]'
NUMBER_ARRAY='[1,2,3,4,5]'
MIXED_ARRAY='["text",123,true,null]'
NESTED_ARRAY='[["a","b"],["c","d"]]'
OBJECT_ARRAY='[{"id":1,"name":"item1"},{"id":2,"name":"item2"}]'
ESCAPED_ARRAY='["contains\\nnewline","has\\ttab"]'
UNICODE_ARRAY='["café","résumé","naïve"]'
EMPTY_ARRAY='[]'
SINGLE_ITEM_ARRAY='["only"]'

# Cron Expressions (valid and invalid)
VALID_CRON="0 12 * * MON-FRI"
VALID_CRON_WITH_SECONDS="*/30 * * * * *"
VALID_CRON_RANGE="0 6-18 * * 1-5"
VALID_CRON_LIST="0 9,12,18 * * MON,WED,FRI"
INVALID_CRON="invalid cron"
DISABLED_CRON=false
NULL_CRON=null
EMPTY_CRON=""
COMPLEX_CRON="*/15 4-23 * * 1,3,5"

# Date Values (various formats)
ISO_DATE="2023-12-31T23:59:59.999Z"
DATE_ONLY="2023-12-31"
DATE_TIME="2023-12-31 23:59:59"
UNIX_TIMESTAMP=1704067200
INVALID_DATE="not a date"
FUTURE_DATE="2099-01-01"
PAST_DATE="1970-01-01"
DATE_WITH_TIMEZONE="2023-12-31T23:59:59+05:30"

# Enum Values
COLOR_ENUM=red
SIZE_ENUM=medium
STATUS_ENUM=active
PRIORITY_ENUM=high
INVALID_ENUM=invalid_option
CASE_INSENSITIVE_ENUM=VALUE
NUMBER_ENUM=1
BOOLEAN_ENUM=true

# Object Values (JSON)
SIMPLE_OBJECT='{"name":"John","age":30}'
NESTED_OBJECT='{"user":{"name":"John","contact":{"email":"john@example.com","phone":"+1234567890"}}}'
OBJECT_WITH_ARRAY='{"tags":["tag1","tag2"],"count":2}'
OBJECT_WITH_NULL='{"name":null,"age":undefined}'
EMPTY_OBJECT='{}'
OBJECT_WITH_SPECIAL_CHARS='{"message":"Hello\\nWorld!","emoji":"👍"}'
OBJECT_WITH_BOOLEAN='{"enabled":true,"visible":false}'
OBJECT_WITH_NUMBER='{"pi":3.14159,"scientific":1.23e5}'

# Custom Transformation Examples
CUSTOM_BASE64="SGVsbG8gV29ybGQh"
CUSTOM_REVERSE="!dlroW olleH"
CUSTOM_UPPERCASE="lowercase text"
CUSTOM_TRUNCATE="This text is too long and should be truncated"
CUSTOM_ENCODED_URL="https%3A%2F%2Fexample.com%2Fpath%3Fparam%3Dvalue"

# Multiple Values (delimiter separated)
MULTIPLE_SEMICOLON="value1;value2;value3"
MULTIPLE_PIPE="option1|option2|option3"
MULTIPLE_NEWLINE="line1
line2
line3"
MULTIPLE_SPECIAL="value1&&value2||value3"

# Time Duration Edge Cases
DURATION_COMPLEX="1y2M3w4d5h6m7s"
DURATION_DECIMAL="1.5h"
DURATION_NEGATIVE="-30m"
DURATION_ZERO="0s"
DURATION_LARGE="99999d"
DURATION_FRACTION="0.5h"

# Path Aliases with Complex Substitutions
RELATIVE_PATH="./config/something"
ABSOLUTE_PATH="/var/log/app"
HOME_PATH="$HOME/logs"
USER_PATH="~/.config/app"
ENV_SUBSTITUTION="${NODE_ENV}/config"
NESTED_SUBSTITUTION="$ROOTDIR/${NODE_ENV}/logs"

# Database Connection Strings (various formats)
MONGODB_URL="mongodb://user:pass@host:port/db?options"
POSTGRESQL_URL="postgresql://user:pass@host:port/db"
REDIS_URL="redis://:password@host:port/db"
MYSQL_URL="mysql://user:pass@host:port/db"
SQLITE_URL="sqlite:path/to/database.db"
COSMOS_URL="mongodb://host:port/db?ssl=true"

# Security Configuration (complex scenarios)
CORS_ORIGINS="https://example.com,https://app.example.com"
RATE_LIMIT_COMPLEX="100;window=15m;skipSuccessfulRequests"
JWT_ALGORITHM="HS256"
BCRYPT_ROUNDS=12
OAUTH_SCOPES="read,write,admin"
API_KEYS="key1,key2,key3"
ENCRYPTION_KEY="32-character-encryption-key-here"

# Feature Flags with Complex Logic
FEATURE_FLAGS='{"newDashboard":true,"betaFeatures":["feature1","feature2"],"experimental":false}'
DARK_MODE_THEME=dark
BETA_FEATURE_ACCESS=false
MAINTENANCE_SCHEDULE="2023-12-31T02:00:00Z"
DEPRECATED_FEATURE_WARNING=true

# Performance Tuning Parameters
CACHE_CONFIG='{"ttl":3600,"maxSize":1000,"strategy":"LRU"}'
POOL_CONFIG='{"min":5,"max":20,"acquireTimeoutMillis":30000}'
QUEUE_CONFIG='{"concurrency":10,"maxAttempts":3,"backoff":"exponential"}'
THROTTLE_CONFIG='{"rateLimit":100,"timeWindow":60000,"burstAllowed":20}'

# Monitoring and Logging
LOG_LEVEL=debug
LOG_FORMAT=json
METRICS_ENABLED=true
HEALTH_CHECK_INTERVAL=30s
PERFORMANCE_SAMPLE_RATE=0.1
ERROR_NOTIFICATION_SLACK="https://hooks.slack.com/services/..."

# Third-party Service Configurations
STRIPE_WEBHOOK_SECRET="whsec_..."
AWS_REGION="us-east-1"
GOOGLE_CLOUD_PROJECT="my-project-id"
TWILIO_ACCOUNT_SID="AC..."
SENTRY_DSN="https://...@sentry.io/..."

DB_USER=admin
DB_PASS=secure_password_123
DB_HOST=localhost
DB_PORT=5432
DB_NAME=myapp_db

USER_NAME=missing-user
DOMAIN=no-domain.tld
# Template String and Variable Substitution Examples
TEMPLATE_URL="https://${API_HOST}:${API_PORT}/v${API_VERSION}"
TEMPLATE_EMAIL="${USER_NAME}@${DOMAIN}.com"
TEMPLATE_FILE_PATH="${HOME_DIR}/logs/${APP_NAME}-${NODE_ENV}.log"
TEMPLATE_DATABASE="mongodb://${DB_USER}:${DB_PASS}@${DB_HOST}:${DB_PORT}/${DB_NAME}"

# Variable Aliases and References
API_HOST_ALIAS=$HOST
API_PORT_ALIAS=$PORT
DB_CONNECTION_ALIAS=$DATABASE_URL
DUMP_SQL_ALIAS="$DATABASE_URL/backups/${MONTH}/59756.sql"
REDIS_CONNECTION_ALIAS=$REDIS_URL
LOG_LEVEL_ALIAS=$LOG_LEVEL
TIMEOUT_ALIAS=$REQUEST_TIMEOUT
HOST=128.184.264.11
PORT=8300
API_VERSION=v6
USER_ID=6978514526
WEBHOOK_HOST=webhook.example.com
WEBHOOK_PORT=443
WEBSOCKET_HOST=websocket.example.com
WEBSOCKET_PORT=8080
BUCKET_NAME=myapp-storage-bucket
ENVIRONMENT=production
APP_VERSION=1.0.0
COMPOSE_FILE=docker-compose.yml
PROJECT_NAME=myapp
NAMESPACE=production
# Complex Template Combinations
FULL_API_ENDPOINT="https://${HOST}:${PORT}/api/${API_VERSION}/users/${USER_ID}"
WEBSOCKET_CONNECTION="ws://${WEBSOCKET_HOST}:${WEBSOCKET_PORT}/${NAMESPACE}"
CLOUD_STORAGE_PATH="s3://${BUCKET_NAME}/${ENVIRONMENT}/${APP_VERSION}/files"
DOCKER_COMPOSE_CMD="docker-compose -f ${COMPOSE_FILE} -p ${PROJECT_NAME} up -d"
ROOTDIR=/usr/tmp
# Environment-Dependent Templates
ENV_CONFIG_PATH="${ROOTDIR}/config/${NODE_ENV}.json"
ENV_FEATURE_FLAGS="${FEATURE_FLAGS_}${NODE_ENV}}"

# Nested Variable Substitution
LOG_FILE=application.log
YEAR=2025
MONTH=08
DAY=19
NESTED_TEMPLATE="$ROOTDIR/${NODE_ENV}/logs/${LOG_FILE}"
DEEP_NESTED="$ROOTDIR/${ENVIRONMENT}/data/${YEAR}/${MONTH}/${DAY}"
CONDITIONAL_NESTED="ftp://${DOMAIN}:${PORT}"

# Array Template Substitutions
USER_1=john.doe
USER_2=jane.smith
USER_3=bob.wilson
STATIC_ROOT=/var/www/static
VERSION=v1.2.3
DOCS_DOMAIN=docs.example.com
WEBHOOK_ID=webhook_12345
SERVICE_HOST=api.example.com
SERVICE_PORT=8080
SERVICE_PATH=/api/v1
MODULE=auth
SERVICE_NAME=user-service
TASK_TYPE=background_job
TEMPLATE_ARRAY='["${USER_1}","${USER_2}","${USER_3}"]'
TEMPLATE_OBJECT={"host":"${DB_HOST}","port":"${DB_PORT}","name":"${DB_NAME}"}
PROTOCOL=https
# Path and URL Templates
TEMPLATE_STATIC_PATH="${STATIC_ROOT}/assets/${VERSION}/"
TEMPLATE_API_DOCS="https://${DOCS_DOMAIN}/api/${API_VERSION}/docs"
TEMPLATE_WEBHOOK="${PROTOCOL}://${WEBHOOK_HOST}:${WEBHOOK_PORT}/webhooks/${WEBHOOK_ID}"
SERVICE_PROTOCOL=https
# Dynamic Configuration Templates
TEMPLATE_SERVICE_URL="${SERVICE_PROTOCOL}://${SERVICE_HOST}:${SERVICE_PORT}${SERVICE_PATH}"
TEMPLATE_CACHE_KEY="cache:${ENVIRONMENT}:${MODULE}:${VERSION}"
TEMPLATE_QUEUE_NAME="${ENVIRONMENT}.${SERVICE_NAME}.${TASK_TYPE}"

# Conditional Template Logic
TEMPLATE_ENV_SUFFIX="${NODE_ENV === 'production' ? 'prod' : 'dev'}"
TEMPLATE_LOG_FILE="${LOG_DIR}/app-${NODE_ENV}-${DATE_FORMAT}.log"
TEMPLATE_DB_NAME="${APP_NAME}_${NODE_ENV}"

# Multi-line Template Strings
ACCOUNT_ID=96916
VERIFICATION_URL=http://example.com
TEMPLATE_EMAIL_BODY="Hello ${USER_NAME},\n\nYour account ${ACCOUNT_ID} has been created.\n\nPlease visit ${VERIFICATION_URL} to verify your email.\n\nBest regards,\n${APP_NAME} Team"
TEMPLATE_SQL_QUERY="SELECT * FROM ${TABLE_NAME} WHERE status = '${STATUS}' AND created_at > '${DATE_THRESHOLD}'"

# URL Construction with Variables
TEMPLATE_OAUTH_URL="${OAUTH_PROVIDER}/oauth/authorize?client_id=${CLIENT_ID}&redirect_uri=${REDIRECT_URI}&scope=${SCOPES}"
TEMPLATE_CALLBACK_URL="${BASE_URL}/auth/${PROVIDER}/callback?state=${STATE}"
TEMPLATE_WEBHOOK_URL="${WEBHOOK_BASE}/${SERVICE_NAME}/${EVENT_TYPE}"

# File System Path Templates
TEMPLATE_UPLOAD_PATH="${UPLOAD_DIR}/${USER_ID}/${YEAR}/${MONTH}/"
TEMPLATE_CONFIG_FILE="${CONFIG_DIR}/${ENVIRONMENT}/${SERVICE}.config"
TEMPLATE_LOG_ROTATION="${LOG_DIR}/${SERVICE}-${DATE}.log"

REGISTRY_HOST=registry.example.com
REGISTRY_PORT=8080
SERVICE_NAME=user-service
VERSION=v1.2.3
SERVICE_HOST=api.example.com
SERVICE_PORT=3000
CHECK_TIMEOUT=30s

# Service Discovery Templates
TEMPLATE_SERVICE_REGISTRY="${REGISTRY_HOST}:${REGISTRY_PORT}/services/${SERVICE_NAME}/${VERSION}"
TEMPLATE_HEALTH_CHECK="http://${SERVICE_HOST}:${SERVICE_PORT}/health?timeout=${CHECK_TIMEOUT}"

# Environment-Specific Aliases
DEV_HOST=dev.example.com
LOCALHOST=example.local
PROD_HOST=example.com
STAGING_HOST=stage.example.com
DEV_PORT=9897
PROD_PORT=9997
STAGING_PORT=9967
DEV_API_URL="${DEV_HOST}:${DEV_PORT}"
PROD_API_URL="${PROD_HOST}:${PROD_PORT}"
STAGING_API_URL="${STAGING_HOST}:${STAGING_PORT}"
LOCAL_API_URL="${LOCALHOST}:${PORT}"

# Template Literals with Expressions
SESSION_PREFIX=dasuiiHmb7678
USER_ID=diskuser
TIMESTAMP=2025-08-19
TEMPLATE_VERSIONED_API="/api/v${API_VERSION_MAJOR}.${API_VERSION_MINOR}"
TEMPLATE_TIMESTAMPED_LOG="${LOG_DIR}/app-${CURRENT_DATE}-${CURRENT_TIME}.log"
TEMPLATE_USER_SESSION="${SESSION_PREFIX}_${USER_ID}_${TIMESTAMP}"

DB_PROTOCOL=postgresql
DB_USER=admin
DB_PASS=secure_password_123
DB_HOST=localhost
DB_PORT=5432
DB_NAME=myapp_db
DB_SSL_ENABLED=true
DB_POOL_SIZE=20
CLOUD_PROVIDER=aws
BUCKET=myapp-storage
CLOUD_REGION=us-east-1
CLOUD_DOMAIN=amazonaws.com
PATH_PREFIX=assets

# Complex Variable Interpolation
TEMPLATE_CONNECTION_STRING="${DB_PROTOCOL}://${DB_USER}:${DB_PASS}@${DB_HOST}:${DB_PORT}/${DB_NAME}?ssl=${DB_SSL_ENABLED}&pool=${DB_POOL_SIZE}"
TEMPLATE_CLOUD_ENDPOINT="${CLOUD_PROVIDER}://${BUCKET}.${CLOUD_REGION}.${CLOUD_DOMAIN}/${PATH_PREFIX}"

FEATURE_NAME=new_dashboard
ENVIRONMENT=production
LIMIT_TYPE=api
MAX_REQUESTS=1000
WINDOW_MINUTES=5
SERVICE=user_service
VERSION=v2.1.0
ENVIRONMENT=production

# Dynamic Feature Configuration
TEMPLATE_FEATURE_FLAG="feature.${FEATURE_NAME}.${ENVIRONMENT}"
TEMPLATE_RATE_LIMIT="${LIMIT_TYPE}:${MAX_REQUESTS}:${WINDOW_MINUTES}"
TEMPLATE_CACHE_NAMESPACE="cache:${SERVICE}:${VERSION}:${ENVIRONMENT}"
